import React, { useState, button } from "react";
import Home from "./Home";

const About = (props) => {
  const arr = ["jaya", "shiva", "sawati"];
  return (
    <>
      <div>THis is About {props.name}</div>
    </>
  );
};

export default About;
